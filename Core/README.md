# Core 目录说明

`Core` 目录存放当前 STM32 工程的核心源码和头文件。

- `Inc/`：公共头文件、模块接口声明、HAL 配置头文件
- `Src/`：主程序入口、外设初始化、中断入口、设备层和协议层实现

当前已经接入的设备层模块包括：

- `zdt_stepper`：张大头步进电机协议层，使用 `USART3`
- `bus_servo`：总线舵机设备层，使用 `UART4`
- `wit_imu`：WIT 标准串口协议陀螺仪协议层，使用 `USART2`
- `ops_sensor`：OPS 全方位定位系统协议层，使用 `UART5`
